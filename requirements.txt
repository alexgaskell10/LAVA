
############# RuleTaker requirements ##############
transformers==2.6.0
git+git://github.com/alontalmor/allennlp.git@upstream-merge-10-4
pandas
jsonschema

networkx

inflect

# Adds an @overrides decorator for better documentation and error checking when using subclasses.
overrides==2.8.0

# just for debuggin in pycharm , TODO remove this for final release...
pytest-pycharm

# Builds our documentation.
# sphinx>=2.1.1
xlrd

rdflib

problog==2.1.0.42
PySDD==0.2.10


# For openattack
pyarrow==2.0.0
datasets==1.14.0
OpenAttack==2.1.1

# To keep consistent with AllenNLP lib, revert to prev transformers version
transformers==2.6.0
torch==1.10.0
tensorflow==2.0.0       # <-- might need to upgrade pip for this to work: pip install -U pip
tensorflow-hub==0.12.0
pyyaml
